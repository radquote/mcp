# Add under ~/.codex/config.toml. Replace <MCP_URL> and <YOUR_API_KEY>.

[mcp_servers.radquote]
command = "npx"
args = [
  "-y",
  "mcp-remote",
  "<MCP_URL>",
  "--header",
  "Authorization: Bearer <YOUR_API_KEY>",
]
